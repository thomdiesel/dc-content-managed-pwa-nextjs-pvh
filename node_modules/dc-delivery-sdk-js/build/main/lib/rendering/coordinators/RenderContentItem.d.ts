import { AxiosInstance, AxiosResponse } from 'axios';
import { ContentClientConfig } from '../../ContentClientConfig';
import { RenderedContentItem } from '../model/RenderedContentItem';
/**
 * @hidden
 */
export declare class RenderContentItem {
    private readonly config;
    private readonly contentClient;
    constructor(config: ContentClientConfig, contentClient: AxiosInstance);
    renderContentItem(contentItemId: string, templateName: string, customParameters?: {
        [id: string]: string;
    }): Promise<RenderedContentItem>;
    getQueryParams(templateName: string, customParameters?: {
        [id: string]: string;
    }): string[][];
    parseResponse(response: AxiosResponse): RenderedContentItem;
}
